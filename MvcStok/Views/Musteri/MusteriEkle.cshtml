@model MvcStok.Models.Entity.TBL_MUSTERILER
@{
    ViewBag.Title = "MusteriEkle";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<form class="form-group" method="post">
    <div>
        <label>MÜŞTERİ AD</label>
        @Html.TextBoxFor(m => m.MUSTERIAD, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.MUSTERIAD, "" , new { @style = "color:blue; font-style:italic" })
    </div>
    <div>
        <label>MÜŞTERİ SOYAD</label>
        <input type="text" class="form-control" name="MUSTERISOYAD" />
    </div>

    @*<div>
        <label>MÜŞTERİ SOYAD</label>
        @Html.TextBoxFor(m=>m.MUSTERISOYAD, new {@class="form-control"})
        @Html.ValidationMessageFor(m => m.MUSTERISOYAD, "", new {@style="font-color:blue; font-style:italic"})
    </div>*@

    <div style="margin-top:20px">
        <button class="btn btn-info" id="btn1">MÜŞTERİ EKLE</button>
    </div>
    <script>
        $('#btn1').click(function (){
            alert('Müşteri Ekleme İşlemi Yapıldı!');
        });
    </script>
</form>

